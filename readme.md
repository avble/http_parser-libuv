# Dependencies
libuv1 and http-parser

``` shell
# install in ubuntu
sudo apt install libhttp-parser-dev libuv1-dev
```

# Compilation
## by make 
``` shell
$ make
```

## by cmake 
``` shell
$ mkdir build 
$ cd build && cmake ..
$ make
```